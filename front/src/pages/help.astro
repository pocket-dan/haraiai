---
import Layout from "@/layouts/Layout.astro";

interface FAQ {
  question: string;
  answer: string;
}

function questionIdOf(i: number): string {
  return "Q" + (i + 1);
}

const FAQs: FAQ[] = [
  {
    question: "清算はできますか？",
    answer:
      "はい、haraiai をお使いのグループで「清算」と送ることで可能です。しかし清算はおすすめしていません。理由は、支払いが少ない側が次回払うようにしてバランスを保つと面倒な清算作業はしなくて済み、お互いが楽だと考えているからです。",
  },
  {
    question: "折半り勘の割合を5:5以外に変更することは出来ますか？",
    answer: "現在、haraiai ではその機能には対応しておりません。",
  },
  {
    question: "3人以上での折半は出来ますか？",
    answer: "現在、haraiai ではその機能には対応しておりません。",
  },
];
---

<Layout
  title="よくある質問"
  description="払い合いに対してよくある質問について"
  path="/help/"
>
  <div class="container">
    <h1>よくある質問</h1>

    <section id="toc">
      <h2>目次</h2>
      <ul>
        {
          FAQs.map((e, i) => (
            <a key={i} href={"#" + questionIdOf(i)}>
              <li>{e.question}</li>
            </a>
          ))
        }
      </ul>
    </section>

    <section id="faq">
      <h2>質問と回答</h2>
      <div class="qa-list">
        {
          FAQs.map((e, i) => (
            <dl id={questionIdOf(i)} key={i} class="qa">
              <dt>
                <li>{e.question}</li>
              </dt>
              <dd>
                <p>{e.answer}</p>
              </dd>
            </dl>
          ))
        }
      </div>

      <p class="note">
        下記に掲載されていない質問や要望がある方は
        <a href="/inquiry">お問い合わせ</a>
        よりお気軽にご連絡ください
      </p>
    </section>
  </div>
</Layout>

<style lang="scss">
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 40px;

    h2 {
      margin-top: 60px;
    }

    .note {
      margin-top: 60px;
    }

    .qa-list {
      margin-top: 40px;

      .qa {
        margin-bottom: 30px;
      }
    }
  }
</style>
